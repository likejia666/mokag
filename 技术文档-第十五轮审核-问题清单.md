# 技术文档-第十五轮审核-问题清单（再次核对版）

**审核日期**：2026-04-05（再次核对）  
**文档版本**：v2.4  
**文档路径**：C:\moka\技术文档.txt  
**审核目的**：对剩余问题进行再次逐条验证

---

## 再次核对结果概览

| 问题编号 | 问题名称 | 验证结果 | 更新说明 |
|---------|---------|---------|---------|
| 问题2 | jobs表与job_levels表的外键关系描述不一致 | ❌ **未解决** | 搜索未找到一对多关系的明确说明 |
| 问题7 | notifications表related_type枚举值与代码实现可能不一致 | ✅ **已解决** | 文档1417-1427行已有完整映射表 |
| 问题8 | operation_logs表敏感字段脱敏规则描述过于笼统 | ⚠️ **部分解决** | 有脱敏原则但缺少具体格式规则 |
| 问题9 | 报名按钮状态机与API返回的报名状态不完全匹配 | ❌ **未解决** | user_application仍为null，缺少字段结构定义 |
| 问题11 | 企业认证页面的字段联动规则与API请求参数不完全匹配 | ❌ **未解决** | 缺少类型切换时清空字段的处理规则 |
| 问题12 | 管理员后台侧边菜单与实际功能模块的权限映射缺失 | ❌ **未解决** | 搜索未找到权限映射表 |
| 问题13 | 报名管理页面的批量操作UI与API设计不完全对应 | ❌ **未解决** | 文档2926行仍为"全选当前页数据" |
| 问题14 | 职位管理页的"已结束"状态与数据库status字段不一致 | ⚠️ **部分解决** | 收藏接口9659行有说明，但职位管理页筛选需确认 |
| 问题15 | 职位发布审核规则中的"认证时间≤30天"判断逻辑不完整 | ⚠️ **部分解决** | 有认证时间说明但计算起点不明确 |
| 问题17 | 报名审核流程中"通过报名"后名额处理的逻辑冲突 | ⚠️ **部分解决** | 有"名额恢复"说明但缺少完整名额管理规则 |
| 问题18 | 5.26职位刷新限流流程中的"当前次数"判断时机不明确 | ⚠️ **部分解决** | 流程图显示先判断后+1，但可更明确 |
| 问题19 | 5.15数据定时清理流程中"草稿自动清理"的时机描述矛盾 | ❌ **未解决** | 未说明自动保存是否更新updated_at |
| 问题20 | 目录编号与实际章节编号不一致 | ❌ **未解决** | 目录4.1是启动页，正文1990行是"4.1 UI设计规范" |
| 问题21 | API章节编号与目录编号不一致 | ⚠️ **部分解决** | 接口标记风格一致 |
| 问题23 | users表is_deleted与is_blacklist的优先级规则描述不清晰 | ⚠️ **部分解决** | 有优先级说明但login_locked_until作为条件描述不够清晰 |
| 问题25 | 报名状态与职位的联动规则未完整定义 | ❌ **未解决** | 缺少职位关闭时的名额处理规则 |
| 问题26 | Token自动续期与设备会话的关联逻辑未明确 | ⚠️ **部分解决** | 有续期说明但多设备场景不完整 |

---

## 最终未解决问题清单（13个）

### ❌ 问题2：jobs表与job_levels表的外键关系描述不一致
**验证依据**：搜索未找到jobs表与job_levels一对多关系的明确说明  
**状态**：未解决

**建议修正**：在jobs表索引部分增加：
> 与job_levels表的一对多关系说明：
> - 一个职位可对应多个工种级别记录(job_levels)
> - 查询职位详情时需JOIN job_levels获取招聘明细

---

### ❌ 问题9：报名按钮状态机与API返回的报名状态不完全匹配
**验证依据**：文档8432行`user_application: null`，未说明字段结构  
**状态**：未解决

**建议修正**：在6.6.4节职位详情API响应中增加user_application字段的完整定义：
```json
"user_application": {
  "application_id": 1,
  "job_level_id": 1,
  "category_id": 1,
  "category_name": "电工",
  "level": "senior",
  "level_name": "大工",
  "status": "applied",
  "employer_message": "明天9点面试",
  "reject_reason": null,
  "created_at": "2024-03-06 10:00:00"
}
```

---

### ❌ 问题11：企业认证页面的字段联动规则与API请求参数不完全匹配
**验证依据**：搜索未找到类型切换时清空字段的处理规则  
**状态**：未解决

**建议修正**：在交互逻辑中增加：
> 【类型切换处理】：
> - 切换认证类型时，已填写的当前类型特有字段自动清空
> - 例如：从"企业"切换到"个人雇主"时，清空企业名称、统一社会信用代码、营业执照、法人资料等字段
> - 仅保留联系人、联系电话等通用字段

---

### ❌ 问题12：管理员后台侧边菜单与实际功能模块的权限映射缺失
**验证依据**：搜索未找到菜单与permission_code的映射表  
**状态**：未解决

**建议修正**：增加权限映射表：
```yaml
【菜单-权限映射】：
- 数据统计：data_stats:view
- 用户管理：user:view / user:edit / user:disable
- 企业认证审核：company:view / company:audit
- 职位管理：job:view / job:audit / job:delete
- 工种管理：category:manage
- 举报处理：report:view / report:handle
- 黑名单管理：blacklist:view / blacklist:manage
- 公告管理：announcement:view / announcement:manage
- 系统广播：broadcast:send
- 帮助中心管理：help:category:manage / help:article:manage
- 违禁词管理：banned_word:manage
- 子管理员管理：sub_admin:view / sub_admin:manage
- 系统配置：system_config:view / system_config:update
- 操作日志：operation_log:view
```

---

### ❌ 问题13：报名管理页面的批量操作UI与API设计不完全对应
**验证依据**：文档2926行`全选：仅全选当前页数据`  
**状态**：未解决

**建议修正**：将UI描述修改为：
> 批量操作区：复选框列表（支持跨页选择）、批量通过按钮、批量拒绝按钮

---

### ❌ 问题19：数据定时清理流程中"草稿自动清理"的时机描述矛盾
**验证依据**：搜索未说明自动保存是否更新updated_at  
**状态**：未解决

**问题描述**：文档2661行说"草稿自动保存：编辑过程中每60秒自动保存草稿"，但未说明是否更新updated_at。如果更新updated_at，长期编辑的草稿永远不会被清理。

**建议修正**：明确草稿清理的特殊情况：
> 【草稿清理特殊规则】：
> - 仅清理`updated_at`早于清理阈值且最近60秒内无自动保存触发的草稿
> - 或增加`last_auto_save_at`字段区分手动保存和自动保存
> - 推荐：自动保存不应更新`updated_at`，只更新业务缓存

---

### ❌ 问题20：目录编号与实际章节编号不一致
**验证依据**：
- 目录13行：`4.1 启动页`
- 正文1990行：`4.1 UI设计规范`
- 正文2057行：`【4.1 启动页 / Splash】`

**状态**：未解决

**建议修正**：调整目录结构：
```yaml
4. 页面设计与交互
   4.1 UI设计规范
   4.2 页面详细设计
      4.2.1 启动页 / Splash
      4.2.2 登录注册页
```

---

### ❌ 问题25：报名状态与职位的联动规则未完整定义
**验证依据**：搜索未找到职位关闭时的名额处理规则  
**状态**：未解决

**问题描述**：职位关闭时，已审核通过的报名名额如何处理未定义。

**建议修正**：增加职位关闭时的名额处理规则：
> 【职位关闭时的报名处理】：
> - 职位被关闭时（任何原因：manual/filled/admin/expired），不自动处理已审核通过的报名记录
> - 已通过的报名：名额已消耗，保留记录供参考
> - 待审核的报名：自动变为无效，applied_count需要回退
> - 被拒绝的报名：保持rejected状态

---

## 部分解决的问题清单（7个，需补充细节）

### ⚠️ 问题8：敏感字段脱敏规则描述过于笼统
**当前状态**：文档839行、1611行有脱敏原则说明，但缺少具体格式规则

**建议补充**：增加具体脱敏格式：
```yaml
【敏感字段脱敏规则】：
- 手机号：记录格式 138****8888（保留前3后4位）
- 邮箱：记录格式 t***@example.com
- 身份证号：记录格式 310***********1234（保留前3后4位）
- 银行卡号：不记录，字段名也不出现
- 密码/token：不记录，字段名也不出现
```

---

### ⚠️ 问题14：职位管理页的"已结束"状态与数据库status字段不一致
**当前状态**：文档9659行收藏接口有说明`status=closed`为前端筛选分组口径，表示非active职位集合，但职位管理页筛选（文档2803-2835行）是否同样处理需确认

**建议补充**：在职位管理页的UI布局中明确：
> 状态筛选标签栏：全部、草稿、招聘中、已暂停、已结束
> 说明："已结束"包括closed和expired状态，对应数据库status IN ('closed','expired')

---

### ⚠️ 问题15：职位发布审核规则中的"认证时间≤30天"判断逻辑不完整
**当前状态**：文档1097行有认证时间说明，但认证时间的计算起点（是认证通过日期还是申请日期）不明确

**建议补充**：增加认证时间计算规则：
> 【认证时间计算规则】：
> - 以companies表的audit_time（认证通过时间）为准
> - 每次认证通过后重新计算30天窗口
> - 认证被拒绝后重新认证：通过时间重置，30天窗口重新计算

---

### ⚠️ 问题17：报名审核流程中"通过报名"后名额处理的逻辑冲突
**当前状态**：文档2924行有"名额恢复"说明，文档8989行提到"名额、状态、通知、联系方式开放状态一致更新"，但缺少完整名额管理规则

**建议补充**：增加完整名额管理规则：
> 【名额管理规则】：
> - 报名时：applied_count + 1，名额被占用
> - 审核拒绝或取消报名：applied_count - 1，名额释放
> - 审核通过：名额保持已占用状态，无需额外操作
> - 职位招满时自动关闭（当所有启用级别的applied_count >= recruit_count时）

---

### ⚠️ 问题18：职位刷新限流流程中的"当前次数"判断时机不明确
**当前状态**：流程图5723-5725行显示先判断(5717-5720)后+1(5724)，但描述可以更明确

**建议补充**：明确判断与计数的先后顺序：
> <判断>
> ├─ 获取当前次数 current_count
> ├─ current_count >= refresh_limit_per_day？
> │   ├─ 是 → 返回错误码6004，【不执行+1操作】
> │   └─ 否 → 继续执行刷新
> ↓
> <执行刷新>
> ├─ Redis计数器 + 1
> ├─ 更新jobs.refreshed_at = 当前时间

---

### ⚠️ 问题21：API章节编号与目录编号不一致
**当前状态**：6.3节用户相关接口都有【新增】标记，这是风格统一的做法，但可能影响阅读

**建议**：保持现状，或在文档开头说明标记风格

---

### ⚠️ 问题23：users表is_deleted与is_blacklist的优先级规则描述不清晰
**当前状态**：文档879行有优先级描述，但将login_locked_until>now()作为固定状态并列不够清晰

**建议补充**：增加计算示例：
```php
if (is_deleted == 2) return '已注销';
if (login_locked_until != null && login_locked_until > now()) return '临时锁定';
if (is_blacklist == 1) return '黑名单';
if (is_deleted == 1) return '注销申请中';
return '正常';
```

---

### ⚠️ 问题26：Token自动续期与设备会话的关联逻辑未明确
**当前状态**：文档5661行说明了同一device_session_id的续期规则，但多设备场景不完整

**建议补充**：增加多设备场景说明：
> 【多设备场景下的Token续期】：
> - 每个设备对应一个独立的device_session_id
> - Token续期仅影响当前请求所属的device_session_id
> - 同一用户不同设备的token互不影响，可各自续期
> - 当设备数量超过3个限制时，最早登录且不活跃的设备会话被自动顶替下线

---

## 新增问题清单（帮助页反馈相关）

### ⚠️ 问题27：缺少管理员查看帮助文章反馈列表的API接口
**验证依据**：
- 文档10062-10078行：只有用户提交反馈的接口 POST /api/help/articles/{id}/feedback
- 文档1873-1890行：有 help_article_feedbacks 表设计（包含is_helpful、reason等字段）
- 缺少管理员查看反馈数据的接口

**状态**：未解决

**问题描述**：help_article_feedbacks 表已设计完整，包含了反馈类型(is_helpful)、原因(reason)等字段，但文档中只有用户提交反馈的API，没有管理员查看反馈列表的API。

**建议修正**：在API文档6.10节（帮助中心接口）中增加管理员接口：
```yaml
【新增管理员接口】：
6.10.21 获取帮助文章反馈列表（管理员）
GET /api/admin/help/article-feedbacks

Query参数：
- article_id: 文章ID（可选，用于筛选特定文章）
- is_helpful: 是否helpful（可选，0/1）
- reason: 反馈原因（可选）
- start_date: 开始日期（可选）
- end_date: 结束日期（可选）
- page: 页码
- page_size: 每页数量

响应：
{
  "code": 200,
  "msg": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "article_id": 10,
        "article_title": "如何发布职位",
        "user_id": 100,
        "user_name": "张三",
        "is_helpful": 0,
        "reason": "内容不完整",
        "created_at": "2024-03-06 10:00:00"
      }
    ],
    "pagination": {
      "page": 1,
      "page_size": 20,
      "total": 100
    }
  }
}
```

---

### ⚠️ 问题28：帮助文章反馈原因枚举值未明确定义
**验证依据**：文档1873-1890行说明 reason 字段为"反馈原因（没帮助时可选）"，文档7191行提到可选原因包括"内容不完整、解决方案无效、看不懂、其他"，但未定义枚举值

**状态**：未解决

**问题描述**：反馈原因有4种选项，但枚举值未明确定义，可能导致前端开发和后端实现不一致。

**建议修正**：在文档中明确定义枚举值：
```yaml
【反馈原因枚举定义】：
字段：help_article_feedbacks.reason
类型：VARCHAR(100)，仅 is_helpful=0 时可选

枚举值：
- "内容不完整"
- "解决方案无效"
- "看不懂"
- "其他"
```

---

### ⚠️ 问题29：help_articles表的helpful_count/not_helpful_count回写时机不明确
**验证依据**：文档7192行提到"同步回写 help_articles.helpful_count / not_helpful_count"，但未说明是同步还是异步处理

**状态**：未解决

**问题描述**：helpful_count 和 not_helpful_count 的回写时机不明确，可能影响数据一致性和性能。

**建议修正**：明确回写机制：
> 【文章反馈计数回写规则】：
> - 每次用户提交/更新反馈时，同步更新 help_articles 表的 helpful_count / not_helpful_count
> - helpful_count = SELECT COUNT(*) FROM help_article_feedbacks WHERE article_id = ? AND is_helpful = 1
> - not_helpful_count = SELECT COUNT(*) FROM help_article_feedbacks WHERE article_id = ? AND is_helpful = 0
> - 建议使用数据库触发器或事务保证一致性

---

### ⚠️ 问题30：重复反馈的helpful_count增减逻辑未定义
**验证依据**：文档1878行提到 UNIQUE KEY (article_id, user_id) 确保同一用户对同一文章仅一条记录，"重复提交时更新原反馈"，但未说明更新时 helpful_count 的增减逻辑

**状态**：未解决

**问题描述**：用户从"有帮助"改为"没帮助"时，helpful_count 应-1，not_helpful_count 应+1。但文档未明确此逻辑。

**建议修正**：增加更新逻辑说明：
> 【重复反馈更新逻辑】：
> - 用户再次提交反馈时，更新 is_helpful 和 reason
> - 如果 is_helpful 从 1 变为 0：helpful_count - 1，not_helpful_count + 1
> - 如果 is_helpful 从 0 变为 1：helpful_count + 1，not_helpful_count - 1
> - 如果 is_helpful 未变化：只更新 reason，计数不变

---

## 问题优先级汇总

### P0级别（必须修复，影响核心功能）：4个
| 问题编号 | 问题名称 | 状态 |
|---------|---------|------|
| 问题9 | 报名按钮状态机与API返回的报名状态不完全匹配 | 未解决 |
| 问题17 | 报名审核流程中"通过报名"后名额处理的逻辑冲突 | 部分解决 |
| 问题25 | 报名状态与职位的联动规则未完整定义 | 未解决 |
| 问题8 | operation_logs表敏感字段脱敏规则描述过于笼统 | 部分解决 |

### P1级别（建议修复，影响开发效率）：13个
| 问题编号 | 问题名称 | 状态 |
|---------|---------|------|
| 问题2 | jobs表与job_levels表的外键关系描述不一致 | 未解决 |
| 问题11 | 企业认证页面的字段联动规则与API请求参数不完全匹配 | 未解决 |
| 问题12 | 管理员后台侧边菜单与实际功能模块的权限映射缺失 | 未解决 |
| 问题13 | 报名管理页面的批量操作UI与API设计不完全对应 | 未解决 |
| 问题14 | 职位管理页的"已结束"状态与数据库status字段不一致 | 部分解决 |
| 问题15 | 职位发布审核规则中的"认证时间≤30天"判断逻辑不完整 | 部分解决 |
| 问题18 | 职位刷新限流流程中的"当前次数"判断时机不明确 | 部分解决 |
| 问题19 | 数据定时清理流程中"草稿自动清理"的时机描述矛盾 | 未解决 |
| 问题20 | 目录编号与实际章节编号不一致 | 未解决 |
| 问题26 | Token自动续期与设备会话的关联逻辑未明确 | 部分解决 |
| 问题27 | 缺少管理员查看帮助文章反馈列表的API接口 | 未解决 |
| 问题29 | help_articles表的helpful_count/not_helpful_count回写时机不明确 | 未解决 |
| 问题30 | 重复反馈的helpful_count增减逻辑未定义 | 未解决 |

### P2级别（可选修复，影响较小）：2个
| 问题编号 | 问题名称 | 状态 |
|---------|---------|------|
| 问题23 | users表is_deleted与is_blacklist的优先级规则描述不清晰 | 部分解决 |
| 问题28 | 帮助文章反馈原因枚举值未明确定义 | 未解决 |

---

## 总结

本次核对结果：
- **已解决**：1个（问题7：notifications表related_type映射表）
- **部分解决**：9个
- **未解决**：11个
- **新增问题**：4个（问题27-30，帮助页反馈相关）
- **剩余问题总计**：**20个**（含部分解决的问题）

---

*本报告由技术文档审核工具自动生成*  
*核对时间：2026-04-05*
